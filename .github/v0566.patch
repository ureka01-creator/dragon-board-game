diff -ruN dragon-v0565-work/README.md dragon-v0566-work/README.md
--- dragon-v0565-work/README.md	2026-08-12 02:56:11.260144666 +0000
+++ dragon-v0566-work/README.md	2026-08-12 02:55:51.654508617 +0000
@@ -1,4 +1,11 @@
-# DRAGON BOARD — Web Prototype V0.5.6.5
+# DRAGON BOARD — Web Prototype V0.5.6.6
+
+
+## V0.5.6.6
+- 주사위 마지막 또르륵 구간의 Y축 중앙 보간과 micro-bounce 제거
+- 주사위는 바닥 높이에서 굴림을 완전히 끝낸 뒤 결과 면을 확정
+- 확정된 주사위만 짧은 직선 이동으로 맵 중앙에 배치한 뒤 결과 확대
+- `굴림 → 확정 → 중앙 이동 → 결과 강조` 순서를 명확하게 분리
 
 ## V0.5.6.5
 - 주사위 확정 직전 별도의 280ms 중앙 이동을 제거
diff -ruN dragon-v0565-work/css/style.css dragon-v0566-work/css/style.css
--- dragon-v0565-work/css/style.css	2026-08-12 02:47:53.287034469 +0000
+++ dragon-v0566-work/css/style.css	2026-08-12 02:55:51.654168102 +0000
@@ -2037,15 +2037,23 @@
 /* V0.5.6.3 — 중앙 지역명 워터마크 완전 제거 */
 .area-watermark { display:none !important; }
 
-/* V0.5.6.5 — 굴림이 중앙 착지로 끝나므로 별도 중앙 이동 없이 즉시 결과 강조 */
+/* V0.5.6.6 — 결과가 확정된 뒤에만 중앙으로 짧고 직선적으로 이동한다. */
+.dice-roller.dice-to-center {
+  display:grid !important;
+  opacity:1 !important;
+  z-index:32 !important;
+  transition:transform 160ms cubic-bezier(.2,.82,.25,1) !important;
+}
+
+/* V0.5.6.6 — 중앙 도착 뒤 결과 강조 */
 .dice-roller.dice-result-show {
   display:grid !important;
   opacity:1 !important;
   z-index:32 !important;
   transform-origin:center center !important;
-  animation:dice-result-punch-v0565 760ms cubic-bezier(.2,.9,.25,1) both !important;
+  animation:dice-result-punch-v0566 760ms cubic-bezier(.2,.9,.25,1) both !important;
 }
-@keyframes dice-result-punch-v0565 {
+@keyframes dice-result-punch-v0566 {
   0%   { transform:translate3d(var(--dice-center-x),var(--dice-center-y),0) rotate(0deg) scale(1); opacity:1; }
   18%  { transform:translate3d(var(--dice-center-x),var(--dice-center-y),0) rotate(-5deg) scale(1.50); }
   34%  { transform:translate3d(var(--dice-center-x),var(--dice-center-y),0) rotate(4deg) scale(1.44); }
diff -ruN dragon-v0565-work/index.html dragon-v0566-work/index.html
--- dragon-v0565-work/index.html	2026-08-12 02:56:11.268144666 +0000
+++ dragon-v0566-work/index.html	2026-08-12 02:55:51.654327301 +0000
@@ -5,7 +5,7 @@
   <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
   <meta name="theme-color" content="#120f0c" />
   <title>Dragon Board Prototype</title>
-  <link rel="stylesheet" href="css/style.css?v=0565" />
+  <link rel="stylesheet" href="css/style.css?v=0566" />
 </head>
 <body>
   <main class="app-shell">
@@ -22,7 +22,7 @@
           <h1>DRAGON<br>BOARD</h1>
           <p class="title-sub">왕국이 멸망하기 전에 봉인을 깨고 드래곤을 쓰러뜨려라.</p>
           <button id="titleStartBtn" class="tap-start" type="button">▶ 화면을 터치해서 시작</button>
-          <div class="version-label">PROTOTYPE V0.5.6.5</div>
+          <div class="version-label">PROTOTYPE V0.5.6.6</div>
         </div>
       </div>
     </section>
@@ -183,12 +183,12 @@
     </div>
   </div>
 
-  <script src="js/heroes.js?v=0565"></script>
-  <script src="js/items.js?v=0565"></script>
-  <script src="js/sprites.js?v=0565"></script>
-  <script src="js/map.js?v=0565"></script>
-  <script src="js/monsters.js?v=0565"></script>
-  <script src="js/events.js?v=0565"></script>
-  <script src="js/game.js?v=0565"></script>
+  <script src="js/heroes.js?v=0566"></script>
+  <script src="js/items.js?v=0566"></script>
+  <script src="js/sprites.js?v=0566"></script>
+  <script src="js/map.js?v=0566"></script>
+  <script src="js/monsters.js?v=0566"></script>
+  <script src="js/events.js?v=0566"></script>
+  <script src="js/game.js?v=0566"></script>
 </body>
 </html>
diff -ruN dragon-v0565-work/js/game.js dragon-v0566-work/js/game.js
--- dragon-v0565-work/js/game.js	2026-08-12 02:47:53.286422411 +0000
+++ dragon-v0566-work/js/game.js	2026-08-12 02:55:51.653589909 +0000
@@ -1,4 +1,4 @@
-// DRAGON BOARD V0.5.6.5
+// DRAGON BOARD V0.5.6.6
 (() => {
   const $ = (sel) => document.querySelector(sel);
   const state = {
@@ -1144,8 +1144,8 @@
       const mapW = mapRect.width;
       const mapH = mapRect.height;
 
-      // V0.5.6.5 — 낙하/바운스의 마지막 구간 자체가 맵 중앙 착지로 끝난다.
-      // 결과 확정 직전에 별도 좌표 보정으로 주사위를 끌어당기지 않는다.
+      // V0.5.6.6 — 굴림 자체는 바닥에서 끝내고, 결과가 확정된 뒤에만 중앙으로 이동한다.
+      // 굴리는 동안 중앙 Y축으로 끌어올리지 않아 마지막 재상승/바운스처럼 보이는 현상을 없앤다.
       const centerX = mapLeft + (mapW - dieSize) / 2;
       const centerY = mapTop + (mapH - dieSize) / 2;
       const startX = fromLeft
@@ -1156,8 +1156,8 @@
       const apexY = mapTop - dieSize * (2.7 + Math.random() * 0.55);
       const groundY = centerY + mapH * (0.08 + Math.random() * 0.05);
       const impactX = centerX - dir * mapW * (0.18 + Math.random() * 0.05);
-      const endX = centerX;
-      const endY = centerY;
+      const rollEndX = centerX - dir * mapW * 0.055;
+      const rollEndY = groundY;
 
       diceRoller.style.setProperty('--dice-center-x', `${centerX}px`);
       diceRoller.style.setProperty('--dice-center-y', `${centerY}px`);
@@ -1172,7 +1172,7 @@
       const reduced = window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;
       if (reduced) {
         setRollingDieFace(finalValue);
-        diceRoller.style.transform = `translate3d(${endX}px, ${endY}px, 0) rotate(0deg) scale(1)`;
+        diceRoller.style.transform = `translate3d(${rollEndX}px, ${rollEndY}px, 0) rotate(0deg) scale(1)`;
         diceRoller.classList.remove('rolling');
         diceRoller.classList.add('landed');
         diceRoller.setAttribute('aria-label', `주사위 결과 ${finalValue}`);
@@ -1232,14 +1232,13 @@
           y = groundY - Math.sin(Math.PI * u) * dieSize * 0.31;
           scale = 1 - Math.sin(Math.PI * u) * 0.018;
         } else {
-          // 마지막 또르륵 구간에서 중앙으로 자연스럽게 수렴한다.
-          // 여기서 x/y를 함께 감속시켜 확정 직전 별도 이동이 보이지 않게 한다.
+          // 마지막 또르륵: 바닥 높이를 그대로 유지한 채 옆으로만 감속한다.
+          // 중앙으로 올라가는 Y축 보간과 micro-bounce를 완전히 제거한다.
           const u = (t - bounce2End) / (1 - bounce2End);
           const fromX = impactX + dir * mapW * 0.135;
           const travel = easeOut(u);
-          x = lerp(fromX, endX, travel);
-          const microBounce = Math.abs(Math.sin(u * Math.PI * 3)) * (1 - u) * dieSize * 0.045;
-          y = lerp(groundY, endY, travel) - microBounce;
+          x = lerp(fromX, rollEndX, travel);
+          y = rollEndY;
           scale = 1;
         }
 
@@ -1264,11 +1263,8 @@
         // 날아가는 동안 면을 바꾸되, 막판에는 최종 면을 먼저 보여줘 눈으로 결과를 따라갈 수 있게 한다.
         if (elapsed - lastFaceAt > (t < 0.72 ? 78 : 118)) {
           lastFaceAt = elapsed;
-          if (t < 0.88) {
-            setRollingDieFace(Math.floor(Math.random() * 6) + 1);
-          } else {
-            setRollingDieFace(finalValue);
-          }
+          // V0.5.6.6: 굴림이 완전히 끝나기 전에는 결과 면을 미리 고정하지 않는다.
+          setRollingDieFace(Math.floor(Math.random() * 6) + 1);
         }
 
         if (!impactDone && t >= impactAt) {
@@ -1286,13 +1282,13 @@
 
         diceAnimationFrame = null;
         setRollingDieFace(finalValue);
-        diceRoller.style.transform = `translate3d(${endX}px, ${endY}px, 0) rotate(${Math.round(angle / 360) * 360}deg) scale(1)`;
+        diceRoller.style.transform = `translate3d(${rollEndX}px, ${rollEndY}px, 0) rotate(${Math.round(angle / 360) * 360}deg) scale(1)`;
         diceRoller.classList.remove('rolling');
         diceRoller.classList.add('landed');
         diceRoller.setAttribute('aria-label', `주사위 결과 ${finalValue}`);
 
-        // 결과 주사위는 사라지지 않는다. 플레이어가 이동을 확정할 때까지 보드 위에 남아 있다.
-        setTimeout(resolve, 180);
+        // 바닥에서 굴림이 끝난 뒤 최종 면을 잠깐 보여줘 '결과 확정'을 먼저 인지시킨다.
+        setTimeout(resolve, 140);
       }
 
       setRollingDieFace(Math.floor(Math.random() * 6) + 1);
@@ -1329,11 +1325,25 @@
     // 결과 확정 즉시 상단은 다시 영웅 턴, MOVE는 전체 남은 횟수를 표시.
     renderAll();
 
-    // V0.5.6.5: 굴림 애니메이션이 이미 맵 중앙에서 끝난다.
-    // 별도의 280ms 중앙 이동 없이, 같은 자리에서 곧바로 결과를 확대한다.
+    // V0.5.6.6: 순서가 명확해야 한다.
+    // 1) 바닥에서 또르르 굴림 종료 → 2) 결과 면 확정 → 3) 확정된 주사위가 중앙으로 짧게 직선 이동 → 4) 결과 강조.
     if (diceRoller && worldMap) {
-      diceRoller.classList.remove('rolling', 'landed', 'settling', 'dice-to-center', 'dice-result-show');
-      setRollingDieFace(result);
+      const boardPanel = worldMap.closest('.board-panel');
+      if (boardPanel) {
+        const panelRect = boardPanel.getBoundingClientRect();
+        const mapRect = worldMap.getBoundingClientRect();
+        const dieSize = Math.max(42, Math.min(58, mapRect.width * 0.09));
+        const centerX = mapRect.left - panelRect.left + (mapRect.width - dieSize) / 2;
+        const centerY = mapRect.top - panelRect.top + (mapRect.height - dieSize) / 2;
+        diceRoller.style.setProperty('--dice-center-x', `${centerX}px`);
+        diceRoller.style.setProperty('--dice-center-y', `${centerY}px`);
+        diceRoller.classList.remove('rolling', 'settling', 'dice-result-show');
+        setRollingDieFace(result);
+        diceRoller.classList.add('landed', 'dice-to-center');
+        diceRoller.style.transform = `translate3d(${centerX}px, ${centerY}px, 0) rotate(0deg) scale(1)`;
+        await waitMs(170);
+        diceRoller.classList.remove('dice-to-center', 'landed');
+      }
       diceRoller.classList.add('dice-result-show');
       await waitMs(760);
       diceRoller.classList.remove('dice-result-show');
