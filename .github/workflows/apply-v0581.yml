name: Apply V0.5.8.1 patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'stage V0.5.8.1 patch workflow'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply V0.5.8.1
        run: |
          git apply --check .github/v0581.patch
          git apply .github/v0581.patch
          git rm .github/v0581.patch .github/workflows/apply-v0581.yml
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md index.html js/game.js
          git commit -m "Patch V0.5.8.1"
          git push
