name: Apply V0.5.6.7 patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'stage V0.5.6.7 patch workflow'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply patch
        run: |
          git apply --check .github/v0567.patch
          git apply .github/v0567.patch
          git rm .github/v0567.patch .github/workflows/apply-v0567.yml
      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md index.html js/game.js
          git commit -m "Patch V0.5.6.7"
          git push
