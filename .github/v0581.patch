diff --git a/README.md b/README.md
index 76b6c5c..9fa8c0b 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,10 @@
-# DRAGON BOARD — Web Prototype V0.5.8.0
+# DRAGON BOARD — Web Prototype V0.5.8.1
+
+
+## V0.5.8.1
+- 전투에서 소비 아이템 사용 시 영웅의 행동 1회를 소모하고 즉시 다음 영웅/몬스터 턴으로 진행
+- 필드에서 주사위를 굴리기 전 회복약·마나 물약을 캐릭터 가방에서 직접 사용 가능
+- 필드 소비 아이템 사용은 월드 이동 턴을 소모하지 않으며, 폭탄·연막탄·집중 비약은 전투 전용 유지
 
 
 ## V0.5.8.0
diff --git a/index.html b/index.html
index 67c615d..7ab55c4 100644
--- a/index.html
+++ b/index.html
@@ -5,7 +5,7 @@
   <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
   <meta name="theme-color" content="#120f0c" />
   <title>Dragon Board Prototype</title>
-  <link rel="stylesheet" href="css/style.css?v=0580" />
+  <link rel="stylesheet" href="css/style.css?v=0581" />
 </head>
 <body>
   <main class="app-shell">
@@ -22,7 +22,7 @@
           <h1>DRAGON<br>BOARD</h1>
           <p class="title-sub">왕국이 멸망하기 전에 봉인을 깨고 드래곤을 쓰러뜨려라.</p>
           <button id="titleStartBtn" class="tap-start" type="button">▶ 화면을 터치해서 시작</button>
-          <div class="version-label">PROTOTYPE V0.5.8.0</div>
+          <div class="version-label">PROTOTYPE V0.5.8.1</div>
         </div>
       </div>
     </section>
@@ -183,12 +183,12 @@
     </div>
   </div>
 
-  <script src="js/heroes.js?v=0580"></script>
-  <script src="js/items.js?v=0580"></script>
-  <script src="js/sprites.js?v=0580"></script>
-  <script src="js/map.js?v=0580"></script>
-  <script src="js/monsters.js?v=0580"></script>
-  <script src="js/events.js?v=0580"></script>
-  <script src="js/game.js?v=0580"></script>
+  <script src="js/heroes.js?v=0581"></script>
+  <script src="js/items.js?v=0581"></script>
+  <script src="js/sprites.js?v=0581"></script>
+  <script src="js/map.js?v=0581"></script>
+  <script src="js/monsters.js?v=0581"></script>
+  <script src="js/events.js?v=0581"></script>
+  <script src="js/game.js?v=0581"></script>
 </body>
 </html>
diff --git a/js/game.js b/js/game.js
index 0c49f8f..a1335a8 100644
--- a/js/game.js
+++ b/js/game.js
@@ -1,4 +1,4 @@
-// DRAGON BOARD V0.5.8.0
+// DRAGON BOARD V0.5.8.1
 (() => {
   const $ = (sel) => document.querySelector(sel);
   const state = {
@@ -2309,7 +2309,7 @@
       modal.classList.remove('hidden');
       return;
     }
-    modalContent.innerHTML = `<div class="combat-item-sheet"><div class="status-kicker">COMBAT ITEM</div><h3>🎒 ${hero.name}의 아이템</h3><p>이번 전투 라운드에는 소비 아이템을 1개만 사용할 수 있어. 아이템 사용 후에도 공격/스킬/방어가 가능해.</p><div class="combat-item-grid">${items.map(({item,index}) => `<button type="button" class="transfer-item-row" data-combat-item="${index}"><span>${item.icon}</span><strong>${item.name}</strong><small>${item.desc}</small></button>`).join('')}</div></div>`;
+    modalContent.innerHTML = `<div class="combat-item-sheet"><div class="status-kicker">COMBAT ITEM</div><h3>🎒 ${hero.name}의 아이템</h3><p>소비 아이템 사용은 <strong>이번 전투 행동 1회</strong>를 사용해. 사용이 끝나면 다음 영웅 또는 몬스터 턴으로 넘어가.</p><div class="combat-item-grid">${items.map(({item,index}) => `<button type="button" class="transfer-item-row" data-combat-item="${index}"><span>${item.icon}</span><strong>${item.name}</strong><small>${item.desc}</small></button>`).join('')}</div></div>`;
     modalCloseBtn.textContent = '취소';
     modalContent.querySelectorAll('[data-combat-item]').forEach(btn => btn.addEventListener('click', async () => {
       const index = Number(btn.dataset.combatItem);
@@ -2326,9 +2326,13 @@
         showModal('💨 연막탄', '보스전에서는 연막탄으로 도주할 수 없어.');
         return;
       }
+      // V0.5.8.1: 전투 아이템은 공격/스킬/방어와 동일한 행동 1회다.
+      // 중복 입력을 막고 효과 처리 후 afterHeroAction()으로 다음 차례를 진행한다.
+      c.busy = true;
       heroInventory(hero).splice(index, 1);
       hs.itemUsed = true;
       closeModalPanel();
+      renderCombat();
 
       if (item.effect === 'heal') {
         const before = hero.currentHp; hero.currentHp = Math.min(hero.hp, hero.currentHp + item.value);
@@ -2354,12 +2358,17 @@
         fxAtElement(enemyActorElement(enemy.uid), `-${damage}`, 'damage');
       } else if (item.effect === 'escape') {
         combatLogEntry(`💨 연막탄! ${hero.name}이 전투에서 빠져나간다.`);
+        c.busy = false;
         await endCombat('escaped');
         return;
       }
-      renderCombat();
+
+      hs.acted = true;
+      hero.acPenalty = 0;
+      c.heroActionsThisRound += 1;
+      c.busy = false;
       renderParty();
-      if (!aliveCombatEnemies().length) await endCombat('victory');
+      await afterHeroAction();
     }));
     modal.classList.remove('hidden');
   }
@@ -3984,6 +3993,45 @@
     return text || item.desc || '효과 없음';
   }
 
+  function canUseWorldConsumable(hero, item) {
+    if (!hero || !item || item.type !== 'consumable' || item.effect === 'autoRevive') return false;
+    if (!canUseWorldPrepActions()) return false;
+    const active = getActiveHero();
+    if (!getWorldUnitMembers(active).some(h => h.id === hero.id)) return false;
+    if (item.effect === 'heal') return hero.currentHp < hero.hp;
+    if (item.effect === 'mana') return hero.currentMana !== null && hero.currentMana < maxMana(hero);
+    return false;
+  }
+
+  function useWorldConsumable(hero, inventoryIndex) {
+    if (!hero || !canUseWorldPrepActions()) return false;
+    const active = getActiveHero();
+    if (!getWorldUnitMembers(active).some(h => h.id === hero.id)) return false;
+    const inv = heroInventory(hero);
+    const itemId = inv[inventoryIndex];
+    const item = window.getItemCard?.(itemId);
+    if (!canUseWorldConsumable(hero, item)) return false;
+
+    inv.splice(inventoryIndex, 1);
+    if (item.effect === 'heal') {
+      const before = hero.currentHp;
+      hero.currentHp = Math.min(hero.hp, hero.currentHp + Number(item.value || 0));
+      log(`${item.icon} ${hero.icon} <strong>${hero.name}</strong> · ${item.name} 사용 → HP +${hero.currentHp - before}`);
+    } else if (item.effect === 'mana') {
+      const before = hero.currentMana;
+      hero.currentMana = Math.min(maxMana(hero), hero.currentMana + Number(item.value || 0));
+      log(`🔵 ${hero.icon} <strong>${hero.name}</strong> · ${item.name} 사용 → MANA +${hero.currentMana - before}`);
+    } else {
+      inv.splice(inventoryIndex, 0, itemId);
+      return false;
+    }
+
+    // 필드에서는 주사위를 굴리기 전 준비 행동으로 사용하며 월드 턴은 소모하지 않는다.
+    renderAll();
+    openHeroStatus(hero);
+    return true;
+  }
+
   function openHeroStatus(hero) {
     if (!hero) return;
     const totalAc = Math.max(1, hero.ac + equipmentStat(hero, 'ac') - (hero.acPenalty || 0));
@@ -3995,18 +4043,23 @@
     const inv = heroInventory(hero);
     const active = getActiveHero();
     const canEquipNow = canUseWorldPrepActions() && getWorldUnitMembers(active).some(h => h.id === hero.id);
+    const canUseFieldItemNow = canEquipNow;
     modalContent.innerHTML = `
       <div class="hero-status-sheet">
         <div class="hero-status-top"><div class="hero-status-portrait">${heroSpriteHTML(hero, 'large')}</div><div class="hero-status-title"><div class="status-kicker">CHARACTER STATUS</div><h3>${hero.icon} ${hero.name}</h3><p>${hero.role}</p><div class="status-now">${stateText}${pos ? ` · 📍 ${pos.name}` : ''}</div></div></div>
         <div class="hero-status-bars"><div><span>❤️ HP</span><strong>${hero.currentHp}/${hero.hp}</strong></div>${hero.currentMana !== null ? `<div><span>🔵 MANA</span><strong>${hero.currentMana}/${maxMana(hero)}</strong></div>` : ''}</div>
         <div class="hero-status-stats"><div><span>⚔ 힘</span><strong>${signed(hero.str)}</strong></div><div><span>🏹 민첩</span><strong>${signed(hero.dex)}</strong></div><div><span>✨ 마력</span><strong>${signed(hero.magic)}</strong></div><div><span>🍀 행운</span><strong>${signed(hero.luck)}</strong></div><div><span>🛡 AC</span><strong>${totalAc}${totalAc !== hero.ac ? ` <small>(${hero.ac} ${signed(totalAc-hero.ac)})</small>` : ''}</strong></div><div><span>🎯 장비 명중</span><strong>${signed(attackBonus)}</strong></div><div><span>💥 장비 피해</span><strong>${signed(damageBonus)}</strong></div></div>
         <div class="hero-status-section"><h4>EQUIPMENT</h4><div class="equipment-list"><div><span>⚔ 무기</span><strong>${equipmentName(hero,'weapon')}</strong><small>${equipmentBonusText(hero,'weapon')}</small></div><div><span>🛡 방어구</span><strong>${equipmentName(hero,'armor')}</strong><small>${equipmentBonusText(hero,'armor')}</small></div><div><span>💍 장신구</span><strong>${equipmentName(hero,'accessory')}</strong><small>${equipmentBonusText(hero,'accessory')}</small></div></div></div>
-        <div class="hero-status-section"><h4>PERSONAL BAG · ${inv.length}/${BAG_LIMIT}</h4><div class="personal-bag-list">${inv.length ? inv.map((id,index)=>{const item=window.getItemCard?.(id); if(!item)return ''; const statText=itemStatsText(item); return `<div class="personal-bag-row"><span class="bag-item-icon">${item.icon||'🎁'}</span><div><strong>${item.name}</strong><small>${item.desc}${statText ? ` · ${statText}` : ''}</small></div>${item.type==='equipment' && canEquipNow && canHeroEquip(hero,item) ? `<button type="button" class="text-btn bag-equip-btn" data-equip-index="${index}">장착</button>` : ''}</div>`;}).join('') : '<div class="personal-bag-empty">가방이 비어 있어.</div>'}</div><div class="bag-rule-note">가방은 최대 ${BAG_LIMIT}칸. 전리품은 획득한 영웅의 개인 가방에 들어간다. 다른 영웅에게 주려면 자신의 월드 턴에 ‘아이템 전달’을 사용해야 해.</div></div>
+        <div class="hero-status-section"><h4>PERSONAL BAG · ${inv.length}/${BAG_LIMIT}</h4><div class="personal-bag-list">${inv.length ? inv.map((id,index)=>{const item=window.getItemCard?.(id); if(!item)return ''; const statText=itemStatsText(item); const canUseItem=canUseFieldItemNow && canUseWorldConsumable(hero,item); const fieldOnlyLabel=item.type==='consumable' && !['heal','mana','autoRevive'].includes(item.effect) ? ' · 전투 전용' : ''; return `<div class="personal-bag-row"><span class="bag-item-icon">${item.icon||'🎁'}</span><div><strong>${item.name}</strong><small>${item.desc}${statText ? ` · ${statText}` : ''}${fieldOnlyLabel}</small></div>${item.type==='equipment' && canEquipNow && canHeroEquip(hero,item) ? `<button type="button" class="text-btn bag-equip-btn" data-equip-index="${index}">장착</button>` : ''}${canUseItem ? `<button type="button" class="text-btn bag-use-btn" data-use-index="${index}">사용</button>` : ''}</div>`;}).join('') : '<div class="personal-bag-empty">가방이 비어 있어.</div>'}</div><div class="bag-rule-note">가방은 최대 ${BAG_LIMIT}칸. 회복약·마나 물약은 <strong>주사위를 굴리기 전 필드에서 사용</strong>할 수 있고 월드 턴은 소모하지 않아. 폭탄·연막탄·집중 비약은 전투 전용이야.</div></div>
         <div class="hero-status-section skill-status"><h4>ABILITY</h4><p><strong>패시브</strong> ${hero.passive}</p><p><strong>고유기</strong> ${hero.skill}</p></div>
       </div>`;
     modalContent.querySelectorAll('[data-equip-index]').forEach(btn => btn.addEventListener('click',()=>{
       if(!canUseWorldPrepActions())return; const activeUnit=getWorldUnitMembers(getActiveHero()); if(!activeUnit.some(h=>h.id===hero.id))return; const index=Number(btn.dataset.equipIndex); const itemId=heroInventory(hero)[index]; if(!itemId)return; openEquipComparison(hero,itemId,index);
     }));
+    modalContent.querySelectorAll('[data-use-index]').forEach(btn => btn.addEventListener('click',()=>{
+      const index = Number(btn.dataset.useIndex);
+      useWorldConsumable(hero, index);
+    }));
     modalCloseBtn.textContent='닫기'; modal.classList.remove('party-manage-modal','item-transfer-modal'); modal.classList.add('hero-status-modal'); modal.classList.remove('hidden');
   }
 
